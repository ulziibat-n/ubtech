# Project Specific Rules
- NEVER add `declare(strict_types=1);` to any PHP files.
- ALWAYS apply code changes directly to the respective files immediately. Do not just show the code in the response; use file editing tools (write_to_file, replace_file_content) to implement the changes.
- Follow WordPress Coding Standards (WPCS).
- Use Tailwind CSS for styling.
- Use `site_` prefix for new functions and CPTs as per global rules.
- ALWAYS escape output variables in PHP (e.g., `esc_html()`, `esc_attr()`, `wp_kses_post()`) to prevent XSS.
- NEVER use short ternary operators (`?:`). ALWAYS use full ternary operators (`$a ? $a : $b`) for clarity and standard compliance.
- ALWAYS use specific variable names in PHP (e.g., `$code_title` instead of `$title`) to avoid overriding WordPress global variables.
